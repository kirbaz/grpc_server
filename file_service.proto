syntax = "proto3";

service ArchiveService {
    rpc EvaluateZip(ZipRequest) returns (EvaluationResponse);
}

message ZipRequest {
    bytes zip_archive = 1;
}

message EvaluationResponse {
    int32 evaluation = 1;
}